import { SpaceToPhonePipe } from './space-to-phone.pipe';

describe('SpaceToPhonePipe', () => {
  it('create an instance', () => {
    const pipe = new SpaceToPhonePipe();
    expect(pipe).toBeTruthy();
  });
});
