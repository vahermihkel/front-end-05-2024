import { SlicePaginationPipe } from './slice-pagination.pipe';

describe('SlicePaginationPipe', () => {
  it('create an instance', () => {
    const pipe = new SlicePaginationPipe();
    expect(pipe).toBeTruthy();
  });
});
